# NammaHadu_FSD
Music-CLone App using Mern

This project contains 2 folders
1) Frontend
2) Server

Pre-Requisites
1) Install the required packages by executing - npm install

To run the application
1) In the server folder execute the command - node server.js
2) In the frontend/music-ui execute the command - npm run dev
